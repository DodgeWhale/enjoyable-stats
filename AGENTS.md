Don't use em dashes.

Once a feature or change is near complete, run `go build ./...` and `go test ./...` as a final check, and do not consider the work finished until both pass. Avoid running them after every small intermediate edit.

Keep commit messages high-level and only representative of what's changed, without context-bloat.
